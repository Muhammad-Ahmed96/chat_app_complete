-# = link_to "Home", root_path
-# -if current_user.present? and current_user.is_admin
-#   = link_to "users", users_path
-# = link_to "Channels", channels_path
-# = link_to "Logout", destroy_user_session_path, method: :delete

%nav.navbar.navbar-expand-lg.navbar-dark.bg-primary
  =link_to "Chat App", root_path, class: 'navbar-brand'
  -# %a.navbar-brand{:href => "#"} Chat App
  %button.navbar-toggler{"aria-controls" => "navbarNavAltMarkup", "aria-expanded" => "false", "aria-label" => "Toggle navigation", "data-target" => "#navbarNavAltMarkup", "data-toggle" => "collapse", :type => "button"}
    %span.navbar-toggler-icon
  #navbarNavAltMarkup.collapse.navbar-collapse
    .navbar-nav
      =link_to "Home", root_path, class: 'nav-item nav-link active'
      -if current_user.present? and current_user.is_admin
        = link_to "users", users_path, class: 'nav-item nav-link'
        = link_to "Rooms", channels_path, class: 'nav-item nav-link'
    .ul.ml-auto
      -if current_user.present?
        = link_to "Logout", destroy_user_session_path, method: :delete, class: 'nav-item nav-link text-white'
      -# %a.nav-item.nav-link.disabled{"aria-disabled" => "true", :href => "#", :tabindex => "-1"} Disabled
